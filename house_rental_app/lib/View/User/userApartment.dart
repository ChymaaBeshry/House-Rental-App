import 'package:flutter/material.dart';
class UserApartmentScreen extends StatelessWidget {
  const UserApartmentScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
